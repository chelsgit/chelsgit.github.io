# 個人記帳軟體 — 完整產品規格書

> 最後更新：2026-06-20
> 技術架構：單一 HTML 檔案 / Vanilla JS / Chart.js / localStorage + Google Sheets

---

## 一、產品概述

一款以 MOZE 為參考基礎的個人記帳 Web App，部署於 GitHub Pages。核心訴求：

1. **流水記帳**：快速記錄日常收支
2. **多帳戶管理**：現金、多家銀行、多張信用卡、電子錢包等
3. **信用卡完整管理**：帳單週期、結帳日/繳款日、分期付款自動拆帳
4. **代付與補收款**：幫他人代墊款項，追蹤應收帳款與實際收回狀態
5. **月結算視覺化**：圓餅圖、長條圖、折線圖、預算達成率等完整儀表板
6. **資料彈性**：localStorage 為主、Google Sheets 雲端備份、JSON 匯出入
7. **語音記帳**：Siri → Apple 捷徑 → n8n webhook → Google Sheets → App 同步

---

## 二、技術架構

| 項目 | 選擇 |
|------|------|
| 檔案 | `expense.html`（單一檔案，獨立於現有 `index.html`） |
| 框架 | Vanilla JavaScript（無框架） |
| CSS | 內嵌 CSS3 + CSS Variables + Google Fonts (Noto Sans TC) |
| 圖表 | Chart.js 4.x（CDN） |
| 主要儲存 | `localStorage`（key: `expenseTrackerData`） |
| 雲端備份 | Google Apps Script Web App → Google Sheets |
| 語音入口 | Siri → Apple 捷徑 → n8n webhook → Google Sheets |
| UI 語言 | 繁體中文 |
| 部署 | GitHub Pages（靜態檔案） |

---

## 三、導航結構

### 3.1 四個主要 Tab

| Tab ID | 圖示 | 名稱 | 功能說明 |
|--------|------|------|----------|
| `records` | 📝 | 記帳 | 流水帳新增/編輯/刪除、月份瀏覽、每日分群交易列表 |
| `accounts` | 💳 | 帳戶 | 多帳戶管理、信用卡帳單週期、分期付款追蹤、帳戶間轉帳 |
| `stats` | 📊 | 統計 | 圓餅圖、長條圖、折線圖、預算達成率、信用卡繳款摘要 |
| `settings` | ⚙️ | 設定 | 類別管理、預算設定、代付管理、Google Sheets 同步、n8n 設定、JSON 匯出入 |

### 3.2 導航方式

- **手機（< 768px）**：固定底部 Tab Bar，圖示 + 文字
- **桌面（≥ 768px）**：頂部水平 Tab Bar

```
手機底部 Tab Bar：
┌──────┬──────┬──────┬──────┐
│  📝  │  💳  │  📊  │  ⚙️  │
│ 記帳 │ 帳戶 │ 統計 │ 設定 │
└──────┴──────┴──────┴──────┘
```

---

## 四、資料模型

所有資料存於 `localStorage`，key 為 `expenseTrackerData`，啟動時載入至全域變數 `appData`。

```javascript
let appData = {

  // ============================================
  // 帳戶（支援多種類型）
  // ============================================
  accounts: [
    {
      id: 'acc_1',                  // 唯一 ID（時間戳 + 隨機碼）
      name: '現金',                 // 帳戶名稱
      type: 'cash',                 // 'cash' | 'bank' | 'credit_card' | 'e_wallet'
      icon: '💵',                   // 顯示圖示
      color: '#2563eb',             // 帳戶代表色
      initialBalance: 0,           // 初始餘額（現金/銀行/電子錢包）
      // --- 信用卡專用欄位 ---
      cardClosingDay: null,        // 結帳日（1-28，每月幾號結帳）
      cardDueDay: null,            // 繳款日（1-28，每月幾號繳款）
      cardLimit: null,             // 信用額度
      // --- 排序與狀態 ---
      order: 0,                    // 排序順序
      isArchived: false,           // 是否已封存（不刪除保留歷史）
      createdAt: 1712200000000
    }
  ],

  // ============================================
  // 交易紀錄
  // ============================================
  transactions: [
    {
      id: 'txn_1',
      type: 'expense',             // 'expense' | 'income' | 'transfer'
      amount: 150,                 // 金額（永遠正值）
      categoryId: 'cat_1',         // 分類 ID
      accountId: 'acc_1',          // 來源帳戶
      toAccountId: null,           // 目標帳戶（僅 transfer 使用）
      date: '2026-04-04',          // 交易日期 YYYY-MM-DD
      memo: '午餐便當',            // 備註
      // --- 分期專用 ---
      installmentId: null,         // 所屬分期計畫 ID
      installmentNum: null,        // 第幾期（1-indexed）
      installmentTotal: null,      // 總共幾期
      isAutoGenerated: false,      // 是否為分期自動產生
      // --- 代付專用 ---
      advancePaymentId: null,      // 所屬代付紀錄 ID
      // --- 來源追蹤 ---
      source: 'manual',            // 'manual' | 'siri'
      createdAt: 1712200000000
    }
  ],

  // ============================================
  // 分期付款計畫
  // ============================================
  installments: [
    {
      id: 'inst_1',
      accountId: 'acc_2',          // 哪張信用卡
      categoryId: 'cat_3',         // 分類
      description: 'MacBook Air',  // 品項說明
      totalAmount: 36000,          // 總金額
      numInstallments: 12,         // 分幾期
      amountPerInstallment: 3000,  // 每期金額
      startDate: '2026-04-04',     // 第一期日期
      transactionIds: [],          // 自動產生的交易 ID 列表
      createdAt: 1712200000000
    }
  ],

  // ============================================
  // 代付紀錄（核心新功能）
  // ============================================
  advancePayments: [
    {
      id: 'adv_1',
      payerAccountId: 'acc_1',     // 代付使用的帳戶
      payeeName: '小明',           // 被代付的人
      description: '聚餐代墊',     // 說明
      totalAmount: 1200,           // 代付總金額
      date: '2026-04-04',          // 代付日期
      categoryId: 'cat_1',         // 分類（歸入哪個消費類別）
      // --- 還款追蹤 ---
      status: 'partial',           // 'pending' | 'partial' | 'settled'
      repayments: [                // 補收款紀錄
        {
          id: 'rep_1',
          amount: 600,             // 收回金額
          date: '2026-04-10',      // 收回日期
          toAccountId: 'acc_1',    // 收到哪個帳戶
          memo: '小明轉帳還款'
        }
      ],
      repaidAmount: 600,           // 已收回總額（computed 快取）
      remainingAmount: 600,        // 未收回金額（computed 快取）
      createdAt: 1712200000000
    }
  ],

  // ============================================
  // 分類
  // ============================================
  categories: {
    expense: [
      { id: 'cat_1',  name: '飲食', icon: '🍔', color: '#ef4444' },
      { id: 'cat_2',  name: '交通', icon: '🚗', color: '#f97316' },
      { id: 'cat_3',  name: '購物', icon: '🛒', color: '#eab308' },
      { id: 'cat_4',  name: '居住', icon: '🏠', color: '#22c55e' },
      { id: 'cat_5',  name: '娛樂', icon: '🎮', color: '#3b82f6' },
      { id: 'cat_6',  name: '醫療', icon: '🏥', color: '#8b5cf6' },
      { id: 'cat_7',  name: '教育', icon: '📚', color: '#ec4899' },
      { id: 'cat_8',  name: '日用', icon: '🧴', color: '#14b8a6' },
      { id: 'cat_9',  name: '通訊', icon: '📱', color: '#6366f1' },
      { id: 'cat_10', name: '服飾', icon: '👔', color: '#a855f7' },
      { id: 'cat_11', name: '其他', icon: '📦', color: '#6b7280' }
    ],
    income: [
      { id: 'cat_i1', name: '薪資', icon: '💰', color: '#059669' },
      { id: 'cat_i2', name: '獎金', icon: '🎁', color: '#0891b2' },
      { id: 'cat_i3', name: '投資', icon: '📈', color: '#7c3aed' },
      { id: 'cat_i4', name: '兼職', icon: '💼', color: '#d946ef' },
      { id: 'cat_i5', name: '其他', icon: '💵', color: '#6b7280' }
    ]
  },

  // ============================================
  // 預算
  // ============================================
  budgets: {
    monthlyTotal: 0,               // 每月總預算（0 = 未設定）
    byCategory: {
      // 'cat_1': 5000             // 各類別月預算
    }
  },

  // ============================================
  // 設定
  // ============================================
  settings: {
    defaultAccountId: 'acc_1',     // 預設記帳帳戶
    googleSheetsUrl: '',           // Google Apps Script Web App URL
    googleSheetId: '',             // Google Sheet ID
    n8nWebhookUrl: '',             // n8n webhook URL（Siri 記帳用）
    lastSyncTime: null,            // 上次同步時間
    theme: 'light'                 // 'light' | 'dark'（未來擴充）
  }
};
```

### 4.1 帳戶類型說明

| 類型 | type 值 | 說明 | 專屬欄位 |
|------|---------|------|----------|
| 現金 | `cash` | 實體現金 | `initialBalance` |
| 銀行帳戶 | `bank` | 銀行存款 | `initialBalance` |
| 信用卡 | `credit_card` | 信用卡消費 | `cardClosingDay`, `cardDueDay`, `cardLimit` |
| 電子錢包 | `e_wallet` | LINE Pay、街口等 | `initialBalance` |

### 4.2 餘額計算邏輯

帳戶餘額**不另存**，透過 `computeBalance(accountId)` 即時計算：

```
現金/銀行/電子錢包：
  balance = initialBalance
          + Σ(income transactions for this account)
          - Σ(expense transactions for this account)
          + Σ(transfer IN to this account)
          - Σ(transfer OUT from this account)
          + Σ(代付補收款 received to this account)

信用卡：
  balance = -Σ(expense transactions for this card, unpaid)
  （負值代表欠款；已繳款的透過 transfer 從銀行帳戶轉入信用卡抵銷）
```

---

## 五、功能模組詳細規格

### 5.1 記帳 Tab（Records）

#### 5.1.1 頁面佈局

```
┌─────────────────────────────┐
│  ◀  2026年4月  ▶           │  ← 月份導航
├─────────────────────────────┤
│  收入 $45,000               │  ← 月度摘要列
│  支出 $28,500  結餘 $16,500 │
├─────────────────────────────┤
│  4/4 星期六     日計 -$580  │  ← 日期分群標題
│  ┌───────────────────────┐  │
│  │ 🍔 飲食  午餐便當      │  │  ← 交易列
│  │    💵 現金      -$150  │  │
│  ├───────────────────────┤  │
│  │ 🚗 交通  捷運          │  │
│  │    💳 中信卡     -$30  │  │
│  ├───────────────────────┤  │
│  │ 🍔 飲食  聚餐（代付）🏷️│  │  ← 代付標記
│  │    💵 現金      -$400  │  │
│  └───────────────────────┘  │
│                              │
│  4/3 星期五     日計 +$44,850│
│  ┌───────────────────────┐  │
│  │ 💰 薪資  4月薪水       │  │
│  │    🏦 中信銀行  +$45,000│ │
│  └───────────────────────┘  │
│                              │
│              ＋              │  ← FAB 浮動按鈕
└─────────────────────────────┘
```

#### 5.1.2 新增/編輯記錄 Modal

```
┌─────────────────────────────┐
│  ╳                    儲存  │
├─────────────────────────────┤
│  ┌──────┬──────┬──────┐     │
│  │ 支出 │ 收入 │ 轉帳 │     │  ← 交易類型切換
│  └──────┴──────┴──────┘     │
│                              │
│  $ _______________           │  ← 金額輸入（大字、inputmode=decimal）
│                              │
│  日期  [2026-04-04]    📅    │
│                              │
│  分類                        │
│  ┌────┬────┬────┬────┐      │
│  │ 🍔 │ 🚗 │ 🛒 │ 🏠 │      │  ← 分類圖示選擇（4欄 grid）
│  │飲食│交通│購物│居住│      │
│  ├────┼────┼────┼────┤      │
│  │ 🎮 │ 🏥 │ 📚 │ 📦 │      │
│  │娛樂│醫療│教育│其他│      │
│  └────┴────┴────┴────┘      │
│                              │
│  帳戶  [▼ 現金          ]    │  ← 帳戶下拉選擇
│                              │
│  備註  [_______________]     │
│                              │
│  ☐ 這是代付（幫人代墊）      │  ← 代付勾選
│    被代付人 [___________]    │  ← 代付人名（勾選後顯示）
│                              │
│  ☐ 信用卡分期                │  ← 分期勾選（帳戶為信用卡時顯示）
│    分 [__] 期                │  ← 分期數（勾選後顯示）
│                              │
│  [────────── 儲存 ──────────]│
│  [刪除此筆]                  │  ← 編輯模式才顯示
└─────────────────────────────┘
```

#### 5.1.3 轉帳模式

選擇「轉帳」時，UI 改為：

```
  從  [▼ 中信銀行      ]  ← 來源帳戶
  到  [▼ 現金          ]  ← 目標帳戶
  金額 $ _______________
  日期 [2026-04-04]
  備註 [_______________]
```

常見轉帳場景：
- 銀行 → 現金（提款）
- 銀行 → 信用卡（繳卡費）
- 銀行 → 電子錢包（儲值）

---

### 5.2 帳戶 Tab（Accounts）

#### 5.2.1 頁面佈局

```
┌─────────────────────────────┐
│  淨資產摘要                  │
│  ┌────────┬────────┐        │
│  │ 總資產  │ 總負債  │        │
│  │$156,500│ -$12,300│        │
│  ├────────┴────────┤        │
│  │  淨值  $144,200  │        │
│  └─────────────────┘        │
├─────────────────────────────┤
│  現金與銀行                  │
│  ┌───────────────────────┐  │
│  │ 💵 現金         $3,200│  │
│  │ 🏦 中信銀行   $120,000│  │
│  │ 🏦 玉山銀行    $33,300│  │
│  └───────────────────────┘  │
├─────────────────────────────┤
│  電子錢包                    │
│  ┌───────────────────────┐  │
│  │ 📱 LINE Pay     $2,500│  │
│  │ 📱 街口支付       $800│  │
│  └───────────────────────┘  │
├─────────────────────────────┤
│  信用卡                      │
│  ┌───────────────────────┐  │
│  │ 💳 中信 LINE Pay       │  │
│  │    本期消費 -$5,200     │  │
│  │    結帳日 15號 繳款日 3號│ │
│  │    下次繳款 7/3  $5,200 │  │
│  │    進行中分期 2 筆      │  │
│  │    [查看明細]           │  │
│  ├───────────────────────┤  │
│  │ 💳 國泰 CUBE           │  │
│  │    本期消費 -$7,100     │  │
│  │    結帳日 25號 繳款日 15號││
│  │    下次繳款 7/15 $7,100 │  │
│  │    [查看明細]           │  │
│  └───────────────────────┘  │
├─────────────────────────────┤
│  代付應收款                   │
│  ┌───────────────────────┐  │
│  │ 小明 — 聚餐代墊         │  │
│  │   代付 $1,200 已收 $600 │  │
│  │   待收 $600  ██████░    │  │
│  │   [記錄收款] [查看明細]  │  │
│  ├───────────────────────┤  │
│  │ Amy — 機票代訂          │  │
│  │   代付 $8,500 已收 $0   │  │
│  │   待收 $8,500 ░░░░░░░   │  │
│  │   [記錄收款] [查看明細]  │  │
│  └───────────────────────┘  │
│              ＋              │
└─────────────────────────────┘
```

#### 5.2.2 新增帳戶 Modal

```
┌─────────────────────────────┐
│  新增帳戶              ╳    │
├─────────────────────────────┤
│  帳戶類型                    │
│  ┌──────┬──────┬──────┬────┐│
│  │ 現金 │ 銀行 │ 信用卡│錢包││ ← 類型選擇
│  └──────┴──────┴──────┴────┘│
│                              │
│  名稱  [_______________]     │
│  圖示  [💵 ▼]               │  ← 圖示選擇
│  顏色  [● ▼]               │  ← 色票選擇
│                              │
│  初始餘額  $ ___________     │  ← 現金/銀行/錢包顯示
│                              │
│  --- 信用卡選項（僅信用卡）---│
│  結帳日  [__] 號              │
│  繳款日  [__] 號              │
│  信用額度  $ ___________     │
│                              │
│  [────────── 儲存 ──────────]│
└─────────────────────────────┘
```

#### 5.2.3 信用卡明細 Modal

```
┌─────────────────────────────┐
│  💳 中信 LINE Pay      ╳   │
├─────────────────────────────┤
│  本期帳單（6/16 - 7/15）    │
│  應繳金額  $5,200            │
│  繳款截止  7/3               │
│  ┌───────────────────────┐  │
│  │ 6/20 🍔 飲食 晚餐 $350│  │
│  │ 6/22 🛒 購物 日用 $800│  │
│  │ 6/25 🎮 娛樂 Netflix $390││
│  │ 6/28 MacBook 分期 3/12 $3,000││
│  │ ...                    │  │
│  └───────────────────────┘  │
├─────────────────────────────┤
│  進行中分期                  │
│  ┌───────────────────────┐  │
│  │ MacBook Air            │  │
│  │ $36,000 ÷ 12期         │  │
│  │ 已繳 3期 / 剩餘 9期     │  │
│  │ 每期 $3,000             │  │
│  │ ████████░░░░ 25%       │  │
│  ├───────────────────────┤  │
│  │ iPhone 保護殼          │  │
│  │ $2,400 ÷ 3期           │  │
│  │ 已繳 1期 / 剩餘 2期     │  │
│  │ ████░░░░░░░░ 33%       │  │
│  └───────────────────────┘  │
│  [+ 新增分期]               │
└─────────────────────────────┘
```

---

### 5.3 代付功能（Advance Payments）— 新增功能

#### 5.3.1 功能說明

代付場景：使用者幫朋友/家人/同事代墊款項，之後需要追蹤補收款狀態。

#### 5.3.2 代付流程

```
Step 1: 記帳時勾選「這是代付」
        → 產生一筆 expense 交易（計入帳戶支出）
        → 同時建立 advancePayment 紀錄

Step 2: 代付清單顯示在帳戶 Tab
        → 顯示每筆代付的應收/已收/待收狀態

Step 3: 收到還款時，點擊「記錄收款」
        → 輸入收回金額、收入帳戶、日期
        → 產生一筆 income 交易（計入收入帳戶）
        → 更新 advancePayment 的 repayments 陣列

Step 4: 全額收回後，狀態自動更新為 'settled'
```

#### 5.3.3 代付對帳務的影響

| 時間點 | 記帳動作 | 帳戶影響 |
|--------|---------|---------|
| 代付當下 | expense 交易 | 現金/銀行/卡片 餘額減少 |
| 收到部分還款 | income 交易 | 收款帳戶餘額增加 |
| 全額收回 | 標記 settled | 代付清單中移除（或顯示為已結清） |

#### 5.3.4 統計處理

- 代付支出**預設計入**當月支出（因為錢確實花出去了）
- 補收款**計入**當月收入
- 統計 Tab 可選擇是否排除代付項目以查看「真正的個人消費」

#### 5.3.5 記錄收款 Modal

```
┌─────────────────────────────┐
│  記錄收款 — 小明 聚餐代墊  ╳│
├─────────────────────────────┤
│  代付金額  $1,200            │
│  已收回    $600              │
│  待收回    $600              │
├─────────────────────────────┤
│  本次收款金額                │
│  $ _______________           │
│                              │
│  收款日期  [2026-04-10] 📅   │
│                              │
│  收到帳戶  [▼ 中信銀行    ]  │
│                              │
│  備註  [轉帳還款________]    │
│                              │
│  [────────── 確認收款 ──────]│
│                              │
│  --- 收款歷史 ---            │
│  4/10  $600  中信銀行 轉帳還款│
└─────────────────────────────┘
```

---

### 5.4 信用卡帳單週期邏輯

#### 5.4.1 帳單週期判定

```javascript
function getBillingCycle(transactionDate, closingDay) {
  const txDate = new Date(transactionDate);
  const txDay = txDate.getDate();
  const txMonth = txDate.getMonth();     // 0-indexed
  const txYear = txDate.getFullYear();

  if (txDay <= closingDay) {
    // 交易日 ≤ 結帳日 → 歸入本月帳單
    return {
      cycleStart: new Date(txYear, txMonth - 1, closingDay + 1),
      cycleEnd:   new Date(txYear, txMonth, closingDay),
    };
  } else {
    // 交易日 > 結帳日 → 歸入下月帳單
    return {
      cycleStart: new Date(txYear, txMonth, closingDay + 1),
      cycleEnd:   new Date(txYear, txMonth + 1, closingDay),
    };
  }
}
```

#### 5.4.2 繳款日計算

```javascript
function getPaymentDueDate(cycleEnd, dueDay) {
  // 繳款日在結帳日的「下一個月」的 dueDay
  const year = cycleEnd.getFullYear();
  const month = cycleEnd.getMonth();
  return new Date(year, month + 1, dueDay);
}
```

#### 5.4.3 帳單金額計算

```javascript
function getCreditCardBill(accountId, cycleStart, cycleEnd) {
  return appData.transactions
    .filter(t => t.accountId === accountId
              && t.type === 'expense'
              && t.date >= formatISO(cycleStart)
              && t.date <= formatISO(cycleEnd))
    .reduce((sum, t) => sum + t.amount, 0);
}
```

#### 5.4.4 分期付款自動拆帳

建立分期時自動產生未來交易：

```javascript
function createInstallment(accountId, categoryId, description, totalAmount, numInstallments, startDate) {
  const instId = generateId('inst');
  const perInstallment = Math.floor(totalAmount / numInstallments);
  const lastInstallment = totalAmount - perInstallment * (numInstallments - 1);
  const txnIds = [];

  for (let i = 0; i < numInstallments; i++) {
    const txnId = generateId('txn');
    const txnDate = addMonths(startDate, i);
    const amount = (i === numInstallments - 1) ? lastInstallment : perInstallment;

    appData.transactions.push({
      id: txnId,
      type: 'expense',
      amount: amount,
      categoryId: categoryId,
      accountId: accountId,
      toAccountId: null,
      date: formatISO(txnDate),
      memo: `${description} (${i + 1}/${numInstallments})`,
      installmentId: instId,
      installmentNum: i + 1,
      installmentTotal: numInstallments,
      isAutoGenerated: true,
      advancePaymentId: null,
      source: 'manual',
      createdAt: Date.now()
    });
    txnIds.push(txnId);
  }

  appData.installments.push({
    id: instId,
    accountId, categoryId, description,
    totalAmount, numInstallments,
    amountPerInstallment: perInstallment,
    startDate: formatISO(new Date(startDate)),
    transactionIds: txnIds,
    createdAt: Date.now()
  });

  saveData();
}
```

---

### 5.5 統計 Tab（Stats）— 完整儀表板

#### 5.5.1 頁面佈局

```
┌─────────────────────────────┐
│  ◀  2026年4月  ▶           │  ← 月份導航
├─────────────────────────────┤
│  本月摘要                    │
│  ┌────────┬────────┬──────┐ │
│  │ 收入    │ 支出    │ 結餘 │ │
│  │$45,000 │$28,500 │$16,500││
│  │ ↑12%   │ ↓5%    │      │ │  ← 與上月比較
│  └────────┴────────┴──────┘ │
├─────────────────────────────┤
│  支出分佈                    │
│  ┌─────────────────────┐    │
│  │                     │    │
│  │    🍩 Doughnut      │    │  ← Chart.js 圓餅圖
│  │       Chart         │    │
│  │                     │    │
│  ├─────────────────────┤    │
│  │ 🍔 飲食  35%  $9,975│    │  ← 圖例 + 金額
│  │ 🚗 交通  20%  $5,700│    │
│  │ 🛒 購物  15%  $4,275│    │
│  │ 🎮 娛樂  12%  $3,420│    │
│  │ ...                 │    │
│  └─────────────────────┘    │
├─────────────────────────────┤
│  收支趨勢（近 6 個月）       │
│  ┌─────────────────────┐    │
│  │  █ █ █ █ █ █        │    │  ← Chart.js 長條圖
│  │  █ █ █ █ █ █        │    │    綠 = 收入、紅 = 支出
│  │  1  2  3  4  5  6月 │    │
│  └─────────────────────┘    │
├─────────────────────────────┤
│  消費走勢（近 12 個月）      │
│  ┌─────────────────────┐    │
│  │    📈 Line Chart     │    │  ← Chart.js 折線圖
│  │  ──────────          │    │
│  └─────────────────────┘    │
├─────────────────────────────┤
│  預算達成                    │
│  ┌─────────────────────┐    │
│  │ 總預算 $28,500/$35,000│   │
│  │ ████████████████░ 81%│    │
│  │                      │    │
│  │ 🍔 飲食 $9,975/$12,000│   │
│  │ █████████████░░░ 83% │    │
│  │ 🚗 交通 $5,700/$6,000 │   │
│  │ ██████████████████ 95%│   │
│  │ 🎮 娛樂 $3,420/$5,000 │   │
│  │ ██████████░░░░░░ 68% │    │
│  └─────────────────────┘    │
├─────────────────────────────┤
│  信用卡待繳                  │
│  ┌─────────────────────┐    │
│  │ 💳 中信 LINE Pay     │    │
│  │    $5,200  繳款日 7/3 │    │
│  │    （13天後）         │    │
│  │ 💳 國泰 CUBE         │    │
│  │    $7,100  繳款日 7/15│    │
│  │    （25天後）         │    │
│  └─────────────────────┘    │
├─────────────────────────────┤
│  代付應收摘要                │
│  ┌─────────────────────┐    │
│  │ 待收總額  $9,100      │    │
│  │ 小明  $600            │    │
│  │ Amy   $8,500          │    │
│  └─────────────────────┘    │
└─────────────────────────────┘
```

#### 5.5.2 Chart.js 整合

```html
<!-- CDN 引入 -->
<script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.7/dist/chart.umd.min.js"></script>
```

圖表實例管理（防止 canvas reuse 錯誤）：

```javascript
let chartInstances = {};

function createOrUpdateChart(canvasId, config) {
  if (chartInstances[canvasId]) {
    chartInstances[canvasId].destroy();
  }
  const ctx = document.getElementById(canvasId).getContext('2d');
  chartInstances[canvasId] = new Chart(ctx, config);
}
```

---

### 5.6 設定 Tab（Settings）

#### 5.6.1 設定區塊

| 區塊 | 功能 |
|------|------|
| 分類管理 | 支出/收入分類的新增、編輯、刪除、排序 |
| 預算設定 | 月總預算 + 各分類月預算 |
| 帳戶排序 | 拖曳或上下移動帳戶顯示順序 |
| Google Sheets 同步 | Script URL 設定、同步按鈕、上次同步時間 |
| Siri 語音記帳 | n8n webhook URL、Apple 捷徑設定教學 |
| 資料管理 | JSON 匯出、JSON 匯入、清除所有資料 |

---

## 六、Siri + n8n 語音記帳

### 6.1 整體流程

```
使用者對 Siri 說「記帳 午餐 150」
  │
  ▼
Apple 捷徑（Shortcuts）
  ├── 解析語音文字
  ├── 拆分為：category=飲食, amount=150, memo=午餐
  ├── HTTP POST → n8n webhook URL
  │     Body: {
  │       "category": "飲食",
  │       "amount": 150,
  │       "memo": "午餐",
  │       "date": "2026-04-04",
  │       "source": "siri"
  │     }
  │
  ▼
n8n Workflow（自架）
  ├── Webhook Node：接收 POST
  ├── Function Node：格式化資料、產生 transaction ID
  ├── Google Sheets Node：append row 到「交易紀錄」工作表
  └── Respond to Webhook Node：回傳 { success: true, message: "已記錄 飲食 $150" }
  │
  ▼
Apple 捷徑收到回應
  └── Siri 朗讀「已記錄 飲食 150 元」
  │
  ▼
App 下次開啟
  └── 從 Google Sheets 同步 → 取得 Siri 記的帳
```

### 6.2 Apple 捷徑範本設計

設定頁面提供建立捷徑的步驟說明：

```
1. 開啟「捷徑」App → 新增捷徑
2. 新增動作「要求輸入」
   - 提示：「請說：類別 金額 備註」
   - 類型：文字
3. 新增動作「分割文字」（依空格分割）
4. 新增動作「取得清單項目」×3（分別取第1、2、3項）
5. 新增動作「取得 URL 內容」
   - URL：[你的 n8n webhook URL]
   - 方法：POST
   - 請求主體：JSON
     {
       "category": [第1項],
       "amount": [第2項],
       "memo": [第3項],
       "date": [目前日期 ISO格式],
       "source": "siri"
     }
6. 新增動作「朗讀文字」：已記錄
7. 將捷徑加入 Siri：設定觸發語「記帳」
```

### 6.3 n8n Workflow 設計

```
[Webhook] → [Function: Format] → [Google Sheets: Append] → [Respond to Webhook]
```

**Function Node 程式碼建議：**

```javascript
const categoryMap = {
  '飲食': 'cat_1', '吃飯': 'cat_1', '午餐': 'cat_1', '晚餐': 'cat_1', '早餐': 'cat_1',
  '交通': 'cat_2', '捷運': 'cat_2', '公車': 'cat_2', '計程車': 'cat_2', 'Uber': 'cat_2',
  '購物': 'cat_3', '買東西': 'cat_3',
  '娛樂': 'cat_5', '電影': 'cat_5', '遊戲': 'cat_5',
  '醫療': 'cat_6', '看醫生': 'cat_6',
};

const input = $input.first().json;
const categoryId = categoryMap[input.category] || 'cat_11'; // 預設「其他」
const txnId = 'txn_' + Date.now() + '_' + Math.random().toString(36).substr(2, 5);

return [{
  json: {
    id: txnId,
    type: 'expense',
    amount: parseFloat(input.amount),
    categoryId: categoryId,
    categoryName: input.category,
    accountId: 'acc_1',  // 預設帳戶
    date: input.date || new Date().toISOString().split('T')[0],
    memo: input.memo || '',
    source: 'siri',
    createdAt: Date.now()
  }
}];
```

---

## 七、Google Sheets 同步架構

### 7.1 架構選擇

使用 **Google Apps Script Web App** 作為 API 端點，避免 OAuth2 複雜度。

### 7.2 Google Sheets 工作表結構

| 工作表名稱 | 欄位 |
|-----------|------|
| 交易紀錄 | id, type, amount, categoryId, accountId, toAccountId, date, memo, installmentId, advancePaymentId, source, createdAt |
| 帳戶 | id, name, type, icon, color, initialBalance, cardClosingDay, cardDueDay, cardLimit, order |
| 分期付款 | id, accountId, categoryId, description, totalAmount, numInstallments, amountPerInstallment, startDate |
| 代付紀錄 | id, payerAccountId, payeeName, description, totalAmount, date, categoryId, status, repaidAmount |
| 代付收款 | advancePaymentId, repaymentId, amount, date, toAccountId, memo |
| 分類 | id, name, icon, type, color, sortOrder |
| 設定 | key, value |

### 7.3 Apps Script 範本

設定頁面提供可複製的 Apps Script 程式碼：

```javascript
// doPost: 接收 App 推送的資料
function doPost(e) {
  const data = JSON.parse(e.postData.contents);
  const ss = SpreadsheetApp.getActiveSpreadsheet();

  if (data.action === 'sync_push') {
    // 清除並重寫各工作表
    writeSheet(ss, '交易紀錄', data.data.transactions, [
      'id','type','amount','categoryId','accountId','date','memo','source','createdAt'
    ]);
    writeSheet(ss, '帳戶', data.data.accounts, [
      'id','name','type','initialBalance','cardClosingDay','cardDueDay'
    ]);
    // ... 其他工作表
    return jsonResponse({ success: true, timestamp: new Date().toISOString() });
  }
}

// doGet: 回傳所有資料供 App 拉取
function doGet(e) {
  const ss = SpreadsheetApp.getActiveSpreadsheet();
  const transactions = readSheet(ss, '交易紀錄');
  const accounts = readSheet(ss, '帳戶');
  return jsonResponse({
    success: true,
    data: { transactions, accounts }
  });
}

function writeSheet(ss, name, rows, headers) {
  let sheet = ss.getSheetByName(name);
  if (!sheet) sheet = ss.insertSheet(name);
  sheet.clear();
  sheet.appendRow(headers);
  rows.forEach(r => sheet.appendRow(headers.map(h => r[h] ?? '')));
}

function readSheet(ss, name) {
  const sheet = ss.getSheetByName(name);
  if (!sheet) return [];
  const data = sheet.getDataRange().getValues();
  const headers = data[0];
  return data.slice(1).map(row => {
    const obj = {};
    headers.forEach((h, i) => obj[h] = row[i]);
    return obj;
  });
}

function jsonResponse(obj) {
  return ContentService.createTextOutput(JSON.stringify(obj))
    .setMimeType(ContentService.MimeType.JSON);
}
```

### 7.4 同步策略

| 操作 | 說明 |
|------|------|
| 推送 (Push) | App → Google Sheets。將本地全部資料覆寫到 Sheets |
| 拉取 (Pull) | Google Sheets → App。讀取 Sheets 資料合併到本地 |
| 合併邏輯 | 以 `id` 為主鍵，以 `createdAt` 較大者為準；Siri 來源（`source: 'siri'`）的紀錄直接新增 |
| 衝突 | Last-write-wins（最後寫入者勝） |

---

## 八、函式清單

### 8.1 核心 / 工具函式

| 函式 | 說明 |
|------|------|
| `generateId(prefix)` | 產生唯一 ID：`prefix_timestamp_random` |
| `saveData()` | 序列化 `appData` 寫入 localStorage |
| `loadData()` | 從 localStorage 讀取，缺少欄位補預設值 |
| `showToast(message, type)` | 顯示 toast 通知（success/error/warning） |
| `openModal(id)` | 開啟 modal |
| `closeModal(id)` | 關閉 modal |
| `switchTab(tabName)` | 切換 tab |
| `formatCurrency(amount)` | 格式化金額 `$1,234` |
| `formatDate(dateStr)` | 格式化日期 `04/04 (六)` |
| `computeBalance(accountId)` | 即時計算帳戶餘額 |

### 8.2 記帳模組

| 函式 | 說明 |
|------|------|
| `renderRecords()` | 渲染當月交易列表 |
| `openAddRecordModal(type)` | 開啟新增記錄 modal |
| `openEditRecordModal(txnId)` | 開啟編輯記錄 modal |
| `saveRecord()` | 儲存記錄（新增或更新） |
| `deleteRecord(txnId)` | 刪除記錄 |
| `navigateMonth(delta)` | 月份導航 +/-1 |
| `renderRecordsList(transactions)` | 渲染日期分群的交易列表 |
| `handleTransfer()` | 處理帳戶間轉帳 |

### 8.3 帳戶模組

| 函式 | 說明 |
|------|------|
| `renderAccounts()` | 渲染帳戶列表與淨資產摘要 |
| `openAddAccountModal()` | 新增帳戶 modal |
| `openEditAccountModal(accId)` | 編輯帳戶 modal |
| `saveAccount()` | 儲存帳戶 |
| `deleteAccount(accId)` | 刪除帳戶（檢查是否有關聯交易） |
| `renderCreditCardDetail(accId)` | 渲染信用卡明細 modal |
| `getCreditCardBill(accId, year, month)` | 計算指定週期帳單金額 |
| `getUpcomingPayments()` | 取得所有信用卡待繳列表 |

### 8.4 分期模組

| 函式 | 說明 |
|------|------|
| `openAddInstallmentModal(accId)` | 新增分期 modal |
| `saveInstallment()` | 建立分期 + 自動產生未來交易 |
| `deleteInstallment(instId)` | 刪除分期 + 移除自動產生的交易 |
| `renderInstallments(accId)` | 渲染分期計畫列表 |

### 8.5 代付模組

| 函式 | 說明 |
|------|------|
| `createAdvancePayment(txnData)` | 建立代付紀錄（記帳時勾選觸發） |
| `renderAdvancePayments()` | 渲染代付應收清單 |
| `openRepaymentModal(advId)` | 開啟收款記錄 modal |
| `saveRepayment(advId)` | 記錄一筆收款 |
| `getAdvancePaymentSummary()` | 取得代付摘要（總待收、各人待收） |
| `settleAdvancePayment(advId)` | 手動標記已結清 |

### 8.6 統計模組

| 函式 | 說明 |
|------|------|
| `renderStats()` | 渲染完整統計頁面 |
| `renderMonthlySummary(year, month)` | 月度收支摘要卡片 |
| `renderCategoryPieChart(year, month)` | 圓餅圖 |
| `renderMonthlyBarChart()` | 近 6 個月收支長條圖 |
| `renderTrendLineChart()` | 近 12 個月消費折線圖 |
| `renderBudgetProgress(year, month)` | 預算進度條 |
| `renderCreditCardSummary()` | 信用卡待繳摘要 |
| `renderAdvancePaymentSummary()` | 代付應收摘要 |

### 8.7 設定模組

| 函式 | 說明 |
|------|------|
| `renderSettings()` | 渲染設定頁面 |
| `renderCategoryManager(type)` | 分類管理列表 |
| `saveBudget()` | 儲存預算設定 |
| `exportJSON()` | 匯出全部資料為 .json 檔案 |
| `importJSON()` | 匯入 .json 檔案 |
| `syncToGoogleSheets()` | 推送到 Google Sheets |
| `syncFromGoogleSheets()` | 從 Google Sheets 拉取 |
| `resetAllData()` | 清除所有資料（雙重確認） |

---

## 九、CSS 設計系統

### 9.1 色彩變數

```css
:root {
  /* 品牌色 */
  --primary: #6366f1;        /* 靛藍 - 主色 */
  --primary-dark: #4f46e5;
  --primary-light: #e0e7ff;

  /* 語意色 */
  --income: #059669;         /* 綠 - 收入 */
  --income-light: #d1fae5;
  --expense: #ef4444;        /* 紅 - 支出 */
  --expense-light: #fee2e2;
  --transfer: #0891b2;       /* 青 - 轉帳 */
  --transfer-light: #cffafe;
  --advance: #d946ef;        /* 紫 - 代付 */
  --advance-light: #fae8ff;

  /* 警示色 */
  --warning: #f59e0b;
  --warning-light: #fef3c7;
  --danger: #dc2626;
  --danger-light: #fee2e2;
  --success: #059669;
  --success-light: #d1fae5;

  /* 灰階 */
  --gray-50 ~ --gray-900     /* 同 index.html */

  /* 背景 */
  --bg: #f8fafc;
  --card-bg: #ffffff;
}
```

### 9.2 響應式斷點

| 斷點 | 用途 |
|------|------|
| < 768px | 手機：底部 Tab、單欄佈局、FAB |
| ≥ 768px | 平板/桌面：頂部 Tab、多欄 grid |

---

## 十、實作階段

### Phase 1：骨架與核心基礎設施
- HTML 結構（四個 tab panel + modal 骨架）
- 完整 CSS（響應式、底部/頂部 tab bar、card、modal、toast、FAB）
- JS 核心：`loadData()`, `saveData()`, `switchTab()`, `openModal()`, `showToast()`, `formatCurrency()`, `generateId()`
- 預設資料初始化（分類、現金帳戶）
- **驗收**：App 載入、Tab 切換正常、localStorage 持久化

### Phase 2：記帳 Tab — CRUD
- 新增記錄 modal（支出/收入/轉帳切換、金額、日期、分類選擇、帳戶選擇、備註）
- 代付勾選框 + 被代付人輸入
- 交易列表（依日期分群）+ 月份導航 + 月度摘要
- 編輯/刪除功能
- **驗收**：可新增、編輯、刪除收支紀錄並持久化

### Phase 3：帳戶 Tab — 多帳戶管理
- 帳戶 CRUD（現金/銀行/信用卡/電子錢包）
- 餘額即時計算 + 淨資產摘要
- 信用卡結帳日/繳款日設定
- 帳戶間轉帳（繳卡費、提款、儲值）
- **驗收**：多帳戶運作正常、餘額計算正確

### Phase 4：信用卡帳單與分期
- 帳單週期判定邏輯
- 信用卡明細 modal（當期消費列表、帳單金額）
- 分期付款 CRUD + 自動產生未來交易
- 即將繳款摘要
- **驗收**：帳單週期正確歸類、分期拆帳正確

### Phase 5：代付功能
- 代付紀錄建立（記帳時勾選觸發）
- 代付應收清單（帳戶 Tab 內顯示）
- 收款記錄 modal + 收款歷史
- 狀態自動更新（pending → partial → settled）
- **驗收**：代付 → 部分收款 → 全額結清 流程正常

### Phase 6：統計 Tab — 圖表儀表板
- Chart.js 整合
- 圓餅圖（支出分佈）+ 長條圖（收支比較）+ 折線圖（消費趨勢）
- 預算進度條（總預算 + 各類別）
- 信用卡待繳摘要 + 代付應收摘要
- **驗收**：圖表正確渲染、數據正確

### Phase 7：設定 Tab + 整合
- 分類管理（新增/編輯/刪除）
- 預算設定
- JSON 匯出/匯入
- Google Sheets 同步設定 + 推送/拉取功能
- n8n webhook URL 設定 + Apple 捷徑教學文件
- Siri 來源紀錄合併同步
- 資料重置
- **驗收**：匯出 → 清除 → 匯入還原正常、Google Sheets 同步可用

---

## 十一、驗證清單

- [ ] App 載入正常，四個 Tab 可切換
- [ ] 新增/編輯/刪除收支紀錄，重新整理後 localStorage 持久化
- [ ] 多帳戶（現金、銀行、信用卡、電子錢包）各自餘額正確
- [ ] 帳戶間轉帳正確（銀行→現金、銀行→信用卡繳卡費）
- [ ] 信用卡消費歸屬正確帳單週期
- [ ] 分期付款自動產生未來各月交易
- [ ] 代付 → 部分收款 → 全額結清 完整流程
- [ ] 統計圓餅圖/長條圖/折線圖正確渲染
- [ ] 預算進度條反映正確比例
- [ ] JSON 匯出 → 清除 → 匯入 → 資料完整還原
- [ ] Google Sheets 同步推送/拉取正常
- [ ] 手機開啟底部 Tab Bar 正常、各 modal 不超出螢幕
- [ ] 代付項目在統計中可選擇性排除
